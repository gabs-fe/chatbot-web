# Mini Chatbot 🤖💚
Projeto simples de chatbot utilizando **JavaScript puro**, **HTML** e **CSS**. Ideal para ser hospedado em serviços de hospedagem estática e responder perguntas simples com base em regras definidas.

## ▸ Como Funciona
O chatbot responde a mensagens com base em um conjunto de **regras definidas** no arquivo `rules.js`. Quando o usuário envia uma resposta das opções o chatbot identifica e responde.

## ▸ Estrutura de Arquivos

A estrutura do projeto está organizada da seguinte forma:

chatbot-web/  
├── public/  
│ ├── imgs/ # Armazenamento de imagens (ex: ícones, logo)  
│ ├── pages/ # Páginas HTML  
│ │ └── index.html # Página principal do chatbot  
│ ├── js/ # Scripts JavaScript  
│ │ ├── app.js # Lógica principal do chatbot  
│ │ └── rules.js # Regras de resposta do chatbot  
│ └── styles/ # Arquivos de estilo CSS  
│ └── style.css # Estilos do chatbot  
└── README.md # Documentação do projeto  

## ▸ Personalização

1. **Estilos**: Edite o arquivo `public/styles/style.css` para personalizar a aparência do chatbot.
2. **Lógica de Respostas**: Modifique o arquivo `public/js/rules.js` para alterar ou adicionar novas regras de resposta.
